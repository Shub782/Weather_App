{
    "version":2,
    "build":[
        {
            "src":"api/app.js",
            "use":"@vercel/node"
        }
    ],
    "routes": [
        {
        "src":"/(.*)",
        "dest":"api/app.js"
       }
    ]
}
